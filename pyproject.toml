[project]
name = "fly-in"
version = "0.1.0"
description = "A project about Fly-in Drones"
requires-python = ">=3.12"
dependencies = [
    "flake8>=7.0.0",
    "mypy>=1.9.0",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"